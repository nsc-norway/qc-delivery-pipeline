name: Publish email scripts environment

on:
  push:
    branches:
      - main
    paths:
      - dockerfiles/email-scripts.dockerfile
      - dockerfiles/email-scripts.version
      - .github/workflows/publish-email-scripts-image.yml
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - id: version
        run: echo "value=$(tr -d '\n' < dockerfiles/email-scripts.version)" >> "$GITHUB_OUTPUT"

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: dockerfiles/email-scripts.dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/qc-delivery-email-scripts:${{ steps.version.outputs.value }}
            ghcr.io/${{ github.repository_owner }}/qc-delivery-email-scripts:latest